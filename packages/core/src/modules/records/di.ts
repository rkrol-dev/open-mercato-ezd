import type { AppContainer } from '@open-mercato/shared/lib/di/container'

export function register(container: AppContainer) {
  // Records module services will be registered in Phase 1.
  void container
}
